package com.example.demo.repository;

import com.example.demo.model.IndicadorFrustracao;
import org.springframework.data.jpa.repository.JpaRepository;

public interface IndicadorFrustracaoRepository extends JpaRepository<IndicadorFrustracao, Long> {
}